# env
NODE_ENV=production

# Database (docker-compose 서비스명 postgresql — localhost 아님)
DB_HOST=postgresql
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=todo_db

# JWT
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=24h

# Server
PORT=3000
